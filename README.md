# Why Dictionary - 个人英语词典

一个为你一个人定制的智能英语词典 Web 应用,采用前后端分离架构。

## 项目简介

Why Dictionary 是一个现代化的个人英语词典应用,具有以下特点:

- 🎨 **新海诚风格设计**:柔和的天空渐变背景 + 玻璃拟态效果
- 🔐 **安全认证**:JWT 认证 + 密码加密存储
- 📱 **响应式布局**:适配各种屏幕尺寸
- 🚀 **现代技术栈**:NestJS + React + TypeScript + Prisma

## 技术栈

### 后端
- **框架**: NestJS 10.x
- **语言**: TypeScript 5.x
- **数据库**: SQLite (开发) / PostgreSQL (生产)
- **ORM**: Prisma 5.x
- **认证**: JWT + bcrypt

### 前端
- **框架**: React 18.x
- **语言**: TypeScript 5.x
- **构建工具**: Vite 5.x
- **路由**: React Router 6.x
- **样式**: Tailwind CSS 3.x

## 项目结构

```
dictionary-why/
├── backend/              # 后端服务
│   ├── src/              # 源代码
│   │   ├── auth/         # 认证模块
│   │   ├── prisma/       # 数据库模块
│   │   ├── user/         # 用户模块
│   │   ├── app.module.ts # 根模块
│   │   └── main.ts       # 入口文件
│   ├── prisma/           # Prisma 配置
│   │   └── schema.prisma # 数据模型
│   ├── .env              # 环境变量
│   ├── package.json      # 依赖配置
│   └── README.md         # 后端文档
│
└── frontend/             # 前端应用
    ├── src/              # 源代码
    │   ├── components/   # 可复用组件
    │   ├── pages/        # 页面组件
    │   ├── utils/        # 工具函数
    │   ├── App.tsx       # 根组件
    │   └── main.tsx      # 入口文件
    ├── index.html        # HTML 模板
    ├── package.json      # 依赖配置
    └── README.md         # 前端文档
```

## 快速开始

### 前置要求

- Node.js 18+ 和 npm
- (可选) PostgreSQL 数据库

### 方式一:一键启动(推荐)

```bash
# 1. 首次使用:分别安装前后端依赖
cd backend
npm install
cd ../frontend
npm install
cd ..

# 2. 安装根目录依赖(仅第一次需要)
npm install

# 3. 一键启动前后端开发服务器
npm run dev
```

启动后:
- 后端将在 `http://localhost:3000` 运行
- 前端将在 `http://localhost:5173` 运行
- 打开浏览器访问 `http://localhost:5173` 即可使用

### 方式二:分别启动

如果需要分别启动前后端(例如调试时):

#### 1. 安装依赖

```bash
# 安装后端依赖
cd backend
npm install

# 安装前端依赖
cd ../frontend
npm install
```

#### 2. 配置后端

```bash
cd backend

# 复制环境变量文件(已自动创建)
# 默认使用 SQLite,无需额外配置

# 生成 Prisma 客户端
npm run prisma:generate

# 执行数据库迁移
npm run prisma:migrate
```

#### 3. 启动服务

```bash
# 启动后端(在 backend 目录)
npm run dev
# 后端将在 http://localhost:3000 运行

# 启动前端(在 frontend 目录,新开一个终端)
cd ../frontend
npm run dev
# 前端将在 http://localhost:5173 运行
```

#### 4. 访问应用

打开浏览器访问 `http://localhost:5173`,即可看到登录页面。

## 功能特性

### 当前版本 (v1.1)

- ✅ 用户注册与登录
- ✅ JWT 认证与授权
- ✅ 密码加密存储
- ✅ 自动生成用户头像
- ✅ 新海诚风格 UI
- ✅ 玻璃拟态效果
- ✅ 响应式布局
- ✅ 路由守卫
- ✅ **查词功能**(免费字典 API + 本地缓存)
- ✅ **单词详情展示**(音标、释义、例句)
- ✅ **搜索建议**(前缀匹配)
- ✅ **本地缓存机制**(减少 API 调用)

### 后续计划

- 🔜 中文释义补全(翻译 API 或大模型)
- 🔜 发音功能完善(音频播放)
- 🔜 单词本管理(收藏、分类、标签)
- 🔜 闪卡学习(间隔重复算法)
- 🔜 词生文(AI 生成例句)
- 🔜 词生图(AI 生成配图)
- 🔜 本地词典数据导入
- 🔜 个人资料编辑
- 🔜 主题切换

## API 文档

### 认证接口

#### 注册
```http
POST /api/auth/register
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

#### 登录
```http
POST /api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

#### 获取当前用户
```http
GET /api/auth/me
Authorization: Bearer <token>
```

详细 API 文档请查看 [backend/README.md](backend/README.md)。

## 开发指南

### 后端开发

详见 [backend/README.md](backend/README.md)

### 前端开发

详见 [frontend/README.md](frontend/README.md)

## 数据库

### SQLite (默认)

开发环境默认使用 SQLite,数据库文件位于 `backend/dev.db`。

### PostgreSQL (生产环境)

修改 `backend/.env`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/dictionary_why"
```

修改 `backend/prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

然后重新执行迁移:

```bash
npm run prisma:migrate
```

## 环境变量

### 后端 (.env)

```env
DATABASE_URL="file:./dev.db"
JWT_SECRET="your-secret-key"
JWT_EXPIRES_IN="7d"
PORT=3000
```

## 常见问题

### 1. 如何重置数据库?

```bash
cd backend
rm dev.db  # 删除 SQLite 数据库文件
npm run prisma:migrate  # 重新执行迁移
```

### 2. 如何查看数据库内容?

```bash
cd backend
npm run prisma:studio
```

浏览器会自动打开 Prisma Studio。

### 3. 前端无法连接后端?

确保:
1. 后端服务已启动(http://localhost:3000)
2. 前端 Vite 配置了正确的代理(已配置)
3. 检查浏览器控制台的错误信息

### 4. 登录后刷新页面退出登录?

这是正常的,因为当前版本使用 localStorage 存储 token。刷新页面后,路由守卫会检查 token 是否存在。如果 token 有效,应该能正常访问。

## 许可证

MIT

## 作者

Why Dictionary Team

---

**注意**: 当前版本已实现基础的用户认证和查词功能。单词本、闪卡、AI 增强等功能将在后续版本中实现。
