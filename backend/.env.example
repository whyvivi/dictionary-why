# ========================================
# 后端环境变量配置示例文件
# ========================================

# 数据库连接字符串
# PostgreSQL 示例: postgresql://用户名:密码@localhost:5432/数据库名
# SQLite 示例: file:./dev.db
DATABASE_URL="postgresql://postgres:password@localhost:5432/dictionary_why"

# JWT 密钥(请使用随机生成的强密码)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# JWT 过期时间(默认 7 天)
JWT_EXPIRES_IN="7d"

# 服务端口
# 本地开发默认 3000
# Render 等云平台会通过 PORT 环境变量指定端口
PORT=3000

# ----------------------------------------
# CORS 跨域配置 - 允许访问的前端域名
# ----------------------------------------
# 【本地开发环境】
# - 本地开发时可以不设置此变量
# - 默认允许 http://localhost:5173 访问（Vite 默认端口）
#
# FRONTEND_ORIGIN="http://localhost:5173"
#
# 【Render 部署环境】
# - 部署到 Render 时，必须在 Render 环境变量中设置此变量
# - 变量值应为前端的完整域名（例如 Vercel 部署的地址）
# - 这样可以限制只有指定的前端能访问后端，提高安全性
#
# 示例：
# FRONTEND_ORIGIN="https://your-frontend.vercel.app"

# ----------------------------------------
# 硅基流动 LLM 配置
# ----------------------------------------
# 用于为单词的每个词义自动生成英文例句和中文翻译
#
# 【如何获取 API Key】
# 1. 访问硅基流动官网: https://cloud.siliconflow.cn
# 2. 注册并登录账号
# 3. 进入 API 密钥管理页面: https://cloud.siliconflow.cn/account/ak
# 4. 创建新的 API Key 并复制到下面的配置中
#
# 【本地开发环境】
# - 将下面的 API Key 替换为你自己的真实 API Key
# - 不要将真实的 API Key 提交到 Git 仓库
#
# 【Render 部署环境】
# - 在 Render 的环境变量设置中添加以下三个变量
# - 不要在代码中硬编码 API Key

# 硅基流动 API Key（必填）
SILICONFLOW_API_KEY=sk-your-api-key-here

# 硅基流动 API 基础 URL（可选，默认值如下）
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# LLM 模型 ID（可选，默认使用 DeepSeek R1）
# 其他可用模型请访问: https://cloud.siliconflow.cn/models
LLM_MODEL_ID=deepseek-ai/DeepSeek-R1-0528-Qwen3-8B

