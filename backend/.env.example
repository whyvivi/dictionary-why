# ========================================
# 后端环境变量配置示例文件
# ========================================

# 数据库连接字符串
# PostgreSQL 示例: postgresql://用户名:密码@localhost:5432/数据库名
# SQLite 示例: file:./dev.db
DATABASE_URL="postgresql://postgres:password@localhost:5432/dictionary_why"

# JWT 密钥(请使用随机生成的强密码)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# JWT 过期时间(默认 7 天)
JWT_EXPIRES_IN="7d"

# 服务端口
# 本地开发默认 3000
# Render 等云平台会通过 PORT 环境变量指定端口
PORT=3000

# ----------------------------------------
# CORS 跨域配置 - 允许访问的前端域名
# ----------------------------------------
# 【本地开发环境】
# - 本地开发时可以不设置此变量
# - 默认允许 http://localhost:5173 访问（Vite 默认端口）
#
# FRONTEND_ORIGIN="http://localhost:5173"
#
# 【Render 部署环境】
# - 部署到 Render 时，必须在 Render 环境变量中设置此变量
# - 变量值应为前端的完整域名（例如 Vercel 部署的地址）
# - 这样可以限制只有指定的前端能访问后端，提高安全性
#
# 示例：
# FRONTEND_ORIGIN="https://your-frontend.vercel.app"

