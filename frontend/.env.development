# ========================================
# 前端环境变量配置文件 - 本地开发环境
# ========================================

# 后端 API 根地址（不包含 /api 路径）
# 
# 【本地开发环境】
# - 本地开发时不需要设置此变量
# - 默认使用 http://localhost:3000
# - 代码会自动拼接 /api，最终请求地址为 http://localhost:3000/api
#
# 【测试云端后端】
# - 如果想在本地前端测试云端后端，可以取消下面的注释并设置为云端根域名
# - 例如：VITE_API_BASE_URL="https://dictionary-backend-z9k0.onrender.com"
# - 注意：不要在 URL 后面加 /api，代码会自动拼接
#
# VITE_API_BASE_URL="http://localhost:3000"

