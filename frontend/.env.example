# ========================================
# 前端环境变量配置示例文件
# ========================================
# 
# 说明：
# 1. 复制此文件为 .env.development（本地开发）或 .env.production（生产环境）
# 2. 根据实际情况修改环境变量的值
# 3. .env 文件已在 .gitignore 中忽略，不会提交到 Git

# ----------------------------------------
# 后端 API 根地址（不包含 /api 路径）
# ----------------------------------------
# 
# 【本地开发环境】
# - 本地开发时可以不设置此变量
# - 默认使用 http://localhost:3000
# - 代码会自动拼接 /api，最终请求地址为 http://localhost:3000/api
#
# VITE_API_BASE_URL="http://localhost:3000"
#
# 【Vercel 部署环境】
# - 部署到 Vercel 时，必须在 Vercel 环境变量中设置此变量
# - 变量值应为后端的根域名（例如 Render 部署的地址）
# - 不要包含 /api 路径，代码会自动拼接
# - 示例：VITE_API_BASE_URL="https://dictionary-backend-z9k0.onrender.com"
#
# VITE_API_BASE_URL="https://dictionary-backend-z9k0.onrender.com"

